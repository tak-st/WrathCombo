# ガイド推奨 × 上流実装 × フォーク実装

出典: **Monk Job Guide for Final Fantasy XIV: Dawntrail**（Perfect Balance / Mantra Bot / Nyan Pls 著、コミュニティ製ガイド）
[原文](https://docs.google.com/document/d/1chk4M7FI1fucHpyxVaU30WAEMeHpbUGhvpiGFntAthE/edit)

[MNK-CHANGES.md](MNK-CHANGES.md) の「🟡 実測後に判断」を、ゲームを回す前にガイドで詰められるところまで詰めたもの。
ガイドの推奨は要約であり、原文の再掲ではない。

---

## 結論（先に3行）

1. **B14（桃園を踏鳴と連動）は 🔴 要移植。** 発想は3者とも一致しているが、差は**待つ量**（上流 約1 GCD / フォーク 2 GCD）。紅蓮の窓は11 GCD しかなく、ガイド自身が「ウィーブ枠1つ分早いだけで最後のGCDがバフから外れる」と警告しているため、**1 GCD の差は効く**。実戦検証済みのフォーク実装を優先する。
2. **7秒バースト opener には理論的裏付けがある。** ガイドが5秒版と7秒版を明示的に列挙し、PT構成で選ぶべきとしている。上流には7秒版が無い → **移植価値が確定**。
3. **必殺技の型順序はガイドに明確なルールがある。** ブリッツの種類で変わる。上流は固定、フォークは5択 → どの選択肢がルールに一致するか要確認。

---

## 1. 桃園結義のタイミング（B14）

> **2026-08-19 訂正** — 当初「移植不要」と判定したが、**撤回して 🔴 要移植 とする**。
> 判断の根拠が要約経由の抽出であり、後述のとおり結果が矛盾していたため。
> フォークの実装は実戦で検証されたものなので、そちらを優先する。

### 3者の実装

| | 内容 |
| --- | --- |
| **ガイド推奨** | 紅蓮の極意とダブルウィーブする。踏鳴#1は紅蓮の **1〜4 GCD 前**にウィーブ。<br>**紅蓮は遅めにウィーブしろ**と明示的に警告（早いと窓の最後のGCDにバフが乗らない） |
| **上流実装** | 経路が2本<br>・通常窓 — `IsEvenWindowApproaching()`（紅蓮・桃園が**両方2〜7秒前**）で踏鳴を先に押す<br>・復帰時 — `IsBurstHoldReleaseReady()`（3つ**同時に空いている**）が `CanBrotherhood()` を止め、踏鳴を先に押させる |
| **フォーク実装** | 状況を問わず**踏鳴スタック ≤ 1**（2 GCD 消化後）まで桃園を待たせる |

### 発想は3者とも一致している

「踏鳴を先に始めてからバーストに入る」という順序は**ガイド・上流・フォークで共通**。
上流の `IsBurstHoldReleaseReady()` は `CanBrotherhood()` の先頭で桃園を明示的にブロックしており、
**ダウンタイム復帰時に踏鳴を先行させる**というフォークの意図をそのまま実装している。

差は**待つ量**だけ。

| | 踏鳴 → 桃園の間隔 |
| --- | --- |
| 上流（復帰時） | 踏鳴を押した瞬間に `HasStatusEffect(PerfectBalance)` が立ちブロックが外れる → **約1 GCD** |
| フォーク | スタック ≤ 1 まで待つ → **2 GCD** |

### 判定: 🔴 **要移植（フォーク版を優先）**

紅蓮の窓は **11 GCD** しかなく、ガイド自身が「ウィーブ枠1つ分早いだけで11個目のGCDがバフから外れる」と
警告している。**1 GCD の差は実際に効く**。

バースト前の踏鳴消化数が少ないと、詰め込むべきスキルが窓に収まらなくなる
——この懸念は構造的に妥当であり、フォークが2 GCD 待たせているのはその対処と考えられる。

**移植する内容**（`CanBrotherhood()` に条件を追加する形）:

```text
分岐1  踏鳴中           → 踏鳴スタック ≤ 1
                          （例外: ≤ 2 でも可。124秒以内に桃園使用済み
                            ＋絶空拳バフ残り ≤ 2秒 ＋疾風CD > 17.35 の3条件時）
分岐2  踏鳴中でない     → 踏鳴チャージ 0
分岐3  Blitz残り ≥ 2000ms
```

### 未解決のまま残る点

ガイドの偶数窓の記述について、要約経由の抽出が**矛盾した結果**を返した。

- 「踏鳴#1は紅蓮の1〜4 GCD 前」
- 「桃園・紅蓮を押す時点で踏鳴#1は3 GCD すべて消化済み、ブリッツも撃ち終わり」
- 「2ブリッツが紅蓮の中に入る」

この3つは両立しない（ブリッツ#1がバフ外になる）。奇数窓・偶数窓・opener 別バリアントの
混同が疑われる。**原文の該当セクションを直接読むか、実測しない限り確定できない。**

この不確かさがあるため、実戦で検証済みのフォーク実装を優先する判断とした。

---

## 2. 踏鳴を紅蓮の前に置くか後に置くか（`Fast_Phoenix` / B23 / F7）

| | 内容 |
| --- | --- |
| **ガイド推奨** | 奇数窓（Lunar生成）では **紅蓮が先、踏鳴が後**。「紅蓮は使えるようになったら即押す」→「次の Opo GCD の後に踏鳴」<br>**例外**: 猿神連撃が紅蓮CDの2GCD前に来た場合、その直後に踏鳴を押して双竜脚をバフ窓の外へ押し出す |
| **上流実装** | 経路が2本ある<br>・`ShouldUsePreRoFPerfectBalance()` — 紅蓮CDが2〜7秒のとき踏鳴（＝**紅蓮の前**）<br>・`ShouldUsePostRoFLunarOddPerfectBalance()` — DoubleLunar opener かつ紅蓮バフ中かつ桃園バフ無しのとき踏鳴（＝**紅蓮の後**） |
| **フォーク実装** | `MNK_ST_Fast_Phoenix` で選択<br>・`0`（既定）= 紅蓮後踏鳴 ← **ガイド準拠**<br>・`1` = 紅蓮前踏鳴 |

### 判定: 🟡 **要検証**

フォークの既定値はガイドと一致している。
上流は前後どちらの経路も持っており、**どちらが発火するかは opener 設定と状況次第**。

上流の `IsRoFInPerfectBalanceWindow()`（紅蓮CD 2〜7秒）は、
ガイドが挙げている**例外条項（猿神連撃が2GCD前に来たケース）とほぼ同じ条件**に見える。
つまり上流は「通常は紅蓮後、例外時は紅蓮前」を自動で切り替えている可能性が高い。

**実測すべき点**: 奇数窓で踏鳴が紅蓮の前に出るか後に出るか。
後が基本で、たまに前に出るなら上流はガイド通りに動いており、`Fast_Phoenix` は不要。

---

## 3. 必殺技の型順序（`Phoenix_Order` / B28）

| | 内容 |
| --- | --- |
| **ガイド推奨** | **ブリッツの種類で変わる**<br>・**鳳凰の舞（Rising Phoenix）** — 弱い順に使う。バフから外れる場合に弱い技が外れるようにするため<br>・**爆星脚（Elixir Burst）/ 幻影闘舞（Phantom Rush）** — **Opo-opo GCD のみ**を使う。Opo はチャクラ確定生成＋確定クリティカルで最も強いため |
| **上流実装** | 固定順 |
| **フォーク実装** | `MNK_ST_Phoenix_Order` で5択<br>`1→2→3` / `2→3→1` / `3→2→1` / 直前の型依存 / 現在方向依存 |

### 判定: 🟡 **要確認**

ガイドは**明確なルールを持っている**が、フォークの5択のどれがそれに一致するかは、
実際の型の並び（1/2/3 が Opo/Raptor/Coeurl のどれに対応するか）と突き合わせる必要がある。

**上流の固定順がガイドのルール通りなら、`Phoenix_Order` は不要。**
ただしフォークが5択を用意したのは、固定では対応できない場面があったからのはずなので、
その場面が何だったかを思い出せると判断が早い。

---

## 4. 開幕（F9 / F10 / G4）

| | 内容 |
| --- | --- |
| **ガイド推奨** | 主要な opener を4種類挙げている<br>・DK Opener（**5秒バフ**）<br>・DK Opener（**7秒バフ**）<br>・Demo Opener（**7秒バフ**）※Solar Lunar 用<br>・Double Lunar 系の派生<br>**「現状は多くのジョブが5秒バフ開幕を好む」**としつつ、最適解は**PT構成・撃破時間・ギミック次第**と明記 |
| **上流実装** | 3種（`DoubleLunar` / `SolarLunar` / `BrotherhoodFirst`）をレベル別に。**7秒版は無い。PT構成の検出も無い** |
| **フォーク実装** | 5秒版・7秒版 × 真真・鳳真 の4種 ＋ **PT内の踊り子を検出して自動切替**（既定） |

### 判定: 🔴 **移植価値が確定**

**ガイドが5秒/7秒の両方を明示的に列挙している**ため、7秒バースト opener は理論的裏付けのある選択肢。
上流はこれを持っていない。

踊り子の自動検出については、ガイドは「PT構成次第」と述べるに留まり具体的な判定基準は示していない。
**フォークの「踊り子がいれば7秒」という実装は、ガイドの方針を具体化した独自の判断**。

---

## 5. 乾坤闘気弾・絶空拳（B5 / B29 / A4 / A5）

| | 内容 |
| --- | --- |
| **ガイド推奨** | **乾坤闘気弾** — Opo-opo GCD の後に撃つ。**踏鳴中は絶対に撃たない**（Formless Fist が無駄になる）<br>**絶空拳** — 方向指定の制約が無くどこでも置ける<br>**優先度** — 乾坤の配置を優先。絶空拳は基本CD通りだが、総使用回数が減らないなら保持も可 |
| **上流実装** | `CanFiresReply()` / `CanWindsReply()` で判定、GCDブロック内（必殺技の後） |
| **フォーク実装** | 関数最上部へ引き上げ（B5）。`FiresReply_Order` で3段階（早期／遅延／最大遅延）。`!HasEffect(Buffs.PerfectBalance)` 条件あり |

### 判定: 🟡 **概ね一致、優先順位のみ要検証**

**「踏鳴中は撃たない」はガイド・上流・フォークの3者とも一致**している。
違いは**関数内での優先順位**だけで、実効の挙動が変わるかは実測が要る。

ガイドが「乾坤の配置を優先」と述べている点は、
フォークが乾坤を関数最上部に引き上げた判断と方向性が合っている。

---

## まとめ

| 項目 | 判定 | 根拠 |
| --- | --- | --- |
| B14 桃園と踏鳴の連動 | 🔴 **要移植** | 発想は3者一致。差は遅延量（上流1 GCD / フォーク2 GCD）。11 GCD 枠では1 GCD が効く |
| `Fast_Phoenix` 踏鳴の前後 | 🟡 要検証 | 既定値はガイド準拠。上流も両経路を持つ |
| `Phoenix_Order` 型順序 | 🟡 要確認 | ガイドに明確なルールあり。上流の固定順と照合が必要 |
| 7秒 opener | 🔴 **移植価値あり** | ガイドが7秒バフ開幕を列挙。上流に無い |
| 乾坤・絶空拳 | 🟡 概ね一致 | 「踏鳴中は撃たない」は3者一致。優先順位のみ差 |

**ゲームを回す前に2件（B14 / 7秒opener）の移植価値が確定した。**

B14 は当初「移植不要」と判定したが、ガイドの偶数窓に関する抽出結果が矛盾していたため撤回した。
要約経由の推測より、実戦で検証されたフォーク実装を優先する。
