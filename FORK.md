# このフォークについて

`tak-st/WrathCombo` — [PunishXIV/WrathCombo](https://github.com/PunishXIV/WrathCombo) の個人フォーク。

このドキュメントは、上流への追従作業に入る前に「**何を守るために独自変更を持っているのか**」を確定させるためのもの。

---

## 1. 目的

**モンクのローテーションを、自分の判断基準に合わせて作り替えること。**

上流は「使えるようになったら使う」に寄った実装だが、このフォークは
**「次のバースト窓に合わせて意図的にずらす」判断**をプラグイン側に持ち込んでいる。
独自変更の大半（行数ベースで約9割）がモンクに集中しているのはそのため。

副次的な目的が2つ:

- UI の日本語化（モンク関連プリセット＋共通UI）
- 踊り子・白魔・賢者の細かい挙動調整

---

## 2. 独自変更の棚卸し

> MNK の全変更を項目単位で洗い出したものは [MNK-CHANGES.md](MNK-CHANGES.md) にある。

基準: 上流タグ `1.0.0.14`（`887cf641`, 2025-03-18）との差分 = **10ファイル / +1018 / -358 行**

### A. モンク — バーストタイミング制御 ★中核

フォークの本体。「今使えるか」ではなく「次の紅蓮／桃園に間に合うか」で判断する層を追加している。

| 追加物 | 内容 |
| --- | --- |
| `compareCooldownTime(a1, a2, needTime)` | 2アクションのリキャストを猶予込みで比較 |
| `compareNextBurstTime(action, needTime)` | 次のバースト窓までに間に合うか判定。桃園結義と紅蓮の極意のズレ（54〜66秒）を見て基準を切り替える独自ロジック |
| `MNK_ST_Brotherhood_ROFLastOnly` | 紅蓮の極意の効果中は桃園結義を使わず、終了まで待つ |
| `MNK_ST_Brotherhood_AdjustROF` | 桃園結義をずらした場合、次の紅蓮に合わせ直す |
| `MNK_ST_Fast_Phoenix` | 奇数バーストで踏鳴を紅蓮の前／後どちらに置くか |
| `MNK_ST_Many_PerfectBalance` | チャクラ状態を優先するか、踏鳴の使用回数を優先するか |
| `MNK_ST_FiresReply_Order` | 乾坤闘気弾と必殺技の順序（早期／遅延／最大遅延の3段階） |
| `MNK_ST_Phoenix_Order` | 鳳凰の舞の型順序（5通り。「直前の型依存」「現在方向依存」は独自） |

### B. モンク — 開幕スキル回し ★中核

- 上流の SL（鳳真）/ LL（真真）に加え、**7秒バースト版 `SL7` / `LL7` を新設**
- `MNK_SelectedOpener` の既定値 `4` = **自動**
  → `Opener()` が PT内の踊り子を検出し、いれば `LL7`、いなければ `LL` を返す

「PT構成でバーストタイミングが変わる」という前提をプラグインに持ち込んだ変更。

### C. モンク — 発動条件の設計変更 ★中核

- **上流**: 桃園結義／紅蓮の極意／疾風の極意を「全コンテンツ or ボスのみ」（`*_SubOption`）で制御
- **フォーク**: これを削除し、**対象の最低HP%**（`MNK_ST_Brotherhood_HP` ほか）に置換
  → 「雑魚に強バフを吐かない」判断をコンテンツ種別ではなく対象HPで行う

あわせて追加:

- `positionCheck(actionId, weaveOnly)` — 方向指定が取れるかの事前判定を関数化
- `UsePerfectBalanceAoE(maxPowerSkill)` — AoE側にも踏鳴管理を新設（上流はST側のみ）

### D. 自己回復の判断基準の変更 ★中核

- **上流**: 自分のHP%が閾値以下なら内丹／ブラッドバス
- **フォーク**: **PT平均HP% と自分のHP% の差**が閾値以上なら使用

```csharp
if ((GetPartyAvgHPPercent() - PlayerHealthPercentageHp()) >= 25 && ActionReady(Role.SecondWind))
```

意図: 全体攻撃で全員削れている場面では温存し、**自分だけ削れている**場面で吐く。
AoE側には `MNK_AoE_RiddleOfEarth_Threshold`（金剛の極意をPT平均HPで判断）も追加。

### E. UI の日本語化 ☆中核ではない

- モンクの全プリセット名・説明を日本語化（「連撃」「踏鳴」「紅蓮の極意」等、ゲーム内名称に一致）
- `Window/Functions/UserConfig.cs` の共通UI（コンテンツ種別ラジオ、優先度入力）も日本語化
- プリセットの親子関係も一部変更（乾坤闘気弾を `MNK_STUseROF` の子に、絶空拳を `MNK_STUseROW` の子に）

⚠ **既知の問題**: ImGui のウィジェットIDに日本語が混入している箇所がある。

```csharp
ImGui.InputInt($"###優先度{config.Name}{currentItem}", ref curVal)   // ← 移植時に戻すこと
```

### F. モンク以外の調整 ☆中核ではない

いずれも**新規オプションの追加ではなく、上流プリセットの挙動変更**。

| ジョブ | 内容 |
| --- | --- |
| DNC | パートナー付与とダワーダンスの条件調整、スタンダード／テクニカルステップ中の分岐整理 |
| WHM | ディヴァインベニゾンを2スタック時に自動放出、アフラタス・ミゼリを「神速魔のCDとリリータイマー」で条件付け、AoE側 Lucid の判定を自前実装に |
| SGE | 移動オプションを拡張し、移動中は Psyche とフレグマを優先 |

### G. 上流に吸収済み → 破棄してよい

- `csproj` の `net8.0-windows` → `net9.0` 化（上流も後に `net9.0-windows` 化済み）
- `DalamudPackager` のバージョン更新

---

## 3. 現在の状態（2026-08 時点）

| 項目 | 状態 |
| --- | --- |
| 生きているブランチ | `API`（`4f471d24`）のみ |
| `main` | `API` の祖先。独自コミット 0 |
| `noPotion` / `Branch_1.0.0.8` | `main` に完全包含された古い栞。削除可 |
| フォークの土台 | 上流 `1.0.0.14`（2025-03-18） |
| 上流の現在地 | **`1.0.4.20`**（約16ヶ月ぶんの差） |
| ビルド | **通らない**（エラー121件、すべて ECommons 内） |

### ビルドが通らない理由

インストール済み Dalamud が **15.0.3.1**（2026-08-07）に更新され、`ImGui.NET` が
`Dalamud.Bindings.ImGui` に置き換わっている。サブモジュールの ECommons が
2025-03 時点で固定されているため、参照が解決できない。
**フォークのコードが原因ではない。**

### `API` ブランチの位置づけ

`Kagekazu/WrathCombo` で進んでいた API リファクタを先取りして取り込んだ線
（32コミット中、Kage 19 / Taurenkey 11 / 自分 2）。
このリファクタは**上流本流にすでに吸収済み**で、上流に `API` ブランチはもう存在しない。
→ **経路としての役目は終わっている。上流追従の足がかりにはならない。**

---

## 4. 移植方針

> **2026-08-16 改訂** — 上流最新（1.0.4.20）を実際に調査した結果、区分を見直した。
> 調査の詳細は [UPSTREAM-SYNC.md](UPSTREAM-SYNC.md) を参照。
> **E と C は上流が公式に実装済みだったため、移植不要に変わった。**

### 守るもの / 捨てるもの

| 区分 | 対象 | 判断 |
| --- | --- | --- |
| **実測後に判断** | A バーストタイミング制御 | 上流が同じ問題領域を**設定なしの固定ロジック**で実装済み。素の上流を使い、不満が出た項目だけ再実装する |
| **実測後に判断** | B 7秒バースト opener | 上流の opener 基盤（`PrepullDelays` / `SkipSteps`）で代替できるか要検証 |
| **捨てる** | C HP閾値によるバフ制御 | 上流も `SubOption` を廃し **HP% + ボス判定**に移行済み。上流のほうが柔軟 |
| **移植候補** | D PT平均HPによる自己回復 | 上流は自分のHP%基準のまま。**独自性が残っている唯一の項目** |
| **捨てる** | E 日本語化 | 上流に公式ローカライズ機構（`ja.resx` 3,622エントリ）。乗るだけで日本語になる |
| **要比較** | F DNC / WHM / SGE | 上流も書き換わっている。現物の比較が未了 |
| **捨てる** | G csproj / Packager | 上流に吸収済み |

### 手順

→ **[UPSTREAM-SYNC.md § 7](UPSTREAM-SYNC.md) に改訂版の手順がある。**

要点は「素の上流をまず動かし、実際に不満が出た項目だけ再実装する」こと。

> **なぜマージではなく再実装か**
> `MNK.cs` は上流で 666行 → 487行に縮小し、ロジックは `MNK_Helper.cs`（318行 → 892行）の
> 約40個の述語関数へ分解された。`CustomComboPreset` enum も `Preset` にリネームされ別アセンブリへ移動。
> 差分の再適用（merge / cherry-pick）は成立しない。

---

## 5. 未決事項

- [ ] A のうち、どの項目を実際に再実装するか（素の上流を使ってみてから判断）
- [ ] B の7秒バーストを上流の `PrepullDelays` / `SkipSteps` で表現できるか
- [ ] F（DNC / WHM / SGE）を移植するか。上流の現行実装との比較が未了
- [ ] `noPotion` / `Branch_1.0.0.8` を削除するか
- [ ] 移植先ブランチの名前（`API` は役目を終えているので新規推奨）

解決済み:

- ~~E（日本語化）を移植するか~~ → **不要**。上流の公式ローカライズに乗る
- ~~C（HP閾値制御）を移植するか~~ → **不要**。上流が同等以上の実装を持っている
